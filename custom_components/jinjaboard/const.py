"""Constants for the JinjaBoard integration."""

DOMAIN = "jinjaboard"

# Config-entry `options` key holding the admin-managed list of files/
# directories allowed as a top-level `template` (see template_allowlist.py).
CONF_ALLOWED_TEMPLATES = "allowed_templates"
CONF_PATH = "path"
CONF_IS_DIR = "is_dir"
